# Kustomize overlay for rabbitmq i dev miljø
# Se apps/auth/overlays/dev/kustomization.yaml for detaljerede kommentarer

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base
