# Kubernetes Namespace for prod miljø
# Se clusters/dev/namespace.yaml for detaljerede kommentarer

apiVersion: v1
kind: Namespace
metadata:
  name: prod
  labels:
    environment: prod
    managed-by: flux
